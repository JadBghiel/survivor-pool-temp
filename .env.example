DATABASE_URL="postgresql://user:password@host/dbname"
# migrations need a direct non pooled connection  whihc is the same value as DATABASE_URL
# for a plain local postgres, a different unpooled url for neon
DIRECT_URL="postgresql://user:password@host/dbname"
JWT_SECRET="change_me_to_a_long_random_string"